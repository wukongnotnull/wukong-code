#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if ! command -v python3 >/dev/null 2>&1; then
    exit 0
fi

exec python3 "$SCRIPT_DIR/user-prompt-submit.py" "$(cd "$SCRIPT_DIR/.." && pwd)"
