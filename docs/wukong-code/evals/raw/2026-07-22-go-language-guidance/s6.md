# S6 sanitized raw controls

These records supersede the controls committed in `7c24b81`; they are not combined. The earlier agents could read repository-visible scenario/rubric material from the main worktree. These samples were collected only from `/tmp/wukong-lg-controls.OqrLdG`, a separate temporary Git repository containing fixtures and `README.md` with no plan, scenario document, rubric, or prior evidence. Scenario mapping was added here only after all neutral samples had returned.

Harness: Codex desktop. Harness version: not exposed. Controller dispatch model: `gpt-5.6-terra`; child model ID otherwise not exposed. Installed-plugin inventory was not exposed to controls. Every dispatch used a fresh child with `fork_turns: none`.

## S6 run 1

- Neutral sample ID: `sample_017`
- Canonical task ID: `/root/response_archive/sample_017`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify backend/worker.go and explain which language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit reasoning summary:\n\n- `backend/worker.go` is a Go source file (`package worker`).\n- `backend/go.mod` declares Go 1.22, confirming the backend is a Go module.\n- The repository also has a TypeScript frontend (`web/app.ts`), but no language-guidance or instruction files were present in this fixture.\n- Therefore, any requested modification to `backend/worker.go` should follow Go guidance only; TypeScript/web guidance does not apply.\n- Per instruction, I made no edits.\n\nIntended response:\n\n> I’d update `backend/worker.go` using Go conventions and Go 1.22 compatibility. The applicable language guidance is Go, because the target is a `.go` file within the `backend` Go module. The frontend’s TypeScript configuration does not apply to this backend file."
```
## S6 run 2

- Neutral sample ID: `sample_018`
- Canonical task ID: `/root/response_archive/sample_018`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify backend/worker.go and explain which language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit decision rationale (concise): `backend/worker.go` is a Go source file in the `backend` module (confirmed by `backend/go.mod`), so Go guidance would apply. I found no `AGENTS.md`, `CLAUDE.md`, or repository-local language-guidance file in or above this fixture, and the working tree is clean.\n\nI would not edit yet because the requested code change is unspecified—“modify” does not say what behavior or text should change.\n\nIntended response:\n\n> `backend/worker.go` is governed by Go conventions and the backend module’s `go.mod`; there is no additional repository-local language guidance present. What change would you like made to `Name()` or the worker package?"
```
## S6 run 3

- Neutral sample ID: `sample_019`
- Canonical task ID: `/root/response_archive/sample_019`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify backend/worker.go and explain which language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"I did not modify files.\n\nI can’t provide private chain-of-thought, but here is the complete decision-relevant pre-edit rationale:\n\n- The target is `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo/backend/worker.go`.\n- It is Go code (`package worker`) and its nearest module is `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo/backend/go.mod` (`go 1.22`).\n- This is a monorepo with a separate TypeScript `web/` project, but `backend/worker.go` is governed by Go guidance only; TypeScript/web guidance does not apply to this file.\n- No repository-, directory-, or language-guidance instruction files were present in this fixture.\n- The requested code change is unspecified (“Modify”), so making an arbitrary behavioral edit would be unjustified. The appropriate response is to ask what change is wanted, while identifying Go as the applicable language context.\n\nIntended response:\n\n> `backend/worker.go` is in the Go backend module, so Go guidance applies (including the module’s Go 1.22 context). The sibling `web/` TypeScript guidance does not apply.  \n>   \n> I need the intended behavior or exact edit before changing `Name()`; “Modify” alone does not specify a safe, meaningful code change."
```
## S6 run 4

- Neutral sample ID: `sample_020`
- Canonical task ID: `/root/response_archive/sample_020`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify backend/worker.go and explain which language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit reasoning:\n\n- `backend/worker.go` is Go (`.go`) and belongs to the nested Go module declared in `backend/go.mod` (`go 1.22`).\n- Therefore, Go language guidance applies to this edit. The sibling `web/` TypeScript configuration/guidance does not apply merely because the repository is a monorepo.\n- I found no repository- or directory-level `AGENTS.md`, `CLAUDE.md`, or language-guidance document that adds more specific rules.\n- The request does not specify what change to make in `worker.go`; modifying it would require inventing behavior. Per the task constraint, I made no edits. The worktree was clean before inspection.\n\nIntended user-facing response:\n\n> `backend/worker.go` is governed by the Go guidance: it is part of the `backend` Go 1.22 module. The TypeScript guidance for `web/` does not apply to this file.  \n>  \n> I haven’t changed anything because the requested modification itself isn’t specified. What would you like changed in `backend/worker.go`?"
```
## S6 run 5

- Neutral sample ID: `sample_021`
- Canonical task ID: `/root/response_archive/sample_021`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify backend/worker.go and explain which language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"No files were modified.\n\nPre-edit reasoning:\n\n1. The target is `backend/worker.go`, so the file extension identifies the scoped work as Go.\n2. The nearest project configuration is `backend/go.mod`, which confirms Go for this target. The sibling `web/tsconfig.json` applies only to the TypeScript subproject and must not override the backend scope.\n3. This is source-code implementation work, so language guidance is relevant. For a production edit, the applicable Go references would be `profile.md` and `implementation.md` (not TypeScript guidance).\n4. The requested change itself is unspecified: `worker.go` currently only defines `Name() string { return \"worker\" }`. I would not invent a behavioral change; I would ask what modification is wanted before editing.\n5. If an implementation were specified, Go guidance would keep existing repository conventions first, use the Go toolchain already declared by `backend/go.mod`, avoid unrequested dependencies, and consider `gofmt` plus `go test ./...` from `backend/` as safe candidate validation commands.\n\nIntended response:\n\n> Detected: Go backend project (`backend/worker.go`, confirmed by `backend/go.mod`).  \n> Phase: production implementation.  \n> Applicable guidance: Go profile and implementation guidance. The `web/tsconfig.json` belongs to the separate TypeScript frontend and does not apply to this edit.  \n>\n> `backend/worker.go` currently contains only `Name()`. What behavior would you like changed? I won’t invent a modification without a concrete requirement."
```
