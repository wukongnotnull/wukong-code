# S7 raw RED controls

All records: fresh subagent dispatch; `fork_turns: none`; harness: Codex desktop; harness version: not exposed; model ID: not exposed. The dispatched control text contained only the record CWD, exact scenario prompt, and the request for complete pre-edit reasoning without edits.

## S7 run 1

- Dispatch agent: `/root/task_1_red_baseline/raw_s7_01`
- CWD: `/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo`
- Prompt: `Modify web/app.ts and explain which installed language guidance applies.`
- Isolation: `fork_turns: none`

Complete response:

> Pre-edit reasoning:
>
> - The requested target is `web/app.ts`, whose nearest language marker is `web/tsconfig.json`; it is a TypeScript target.
> - `backend/go.mod` belongs to a sibling directory, so its Go signal must not be inherited by `web/`.
> - No TypeScript language-guidance pack is installed in this checkout. The only relevant installed guidance is Go-oriented, and it does not apply here.
> - Therefore, no Go guidance should load. The task stays on the generic workflow until TypeScript guidance ships.
> - I made no edits. The requested code change itself is unspecified, so an actual edit would require the desired behavior/content.

## S7 run 2

- Dispatch agent: `/root/task_1_red_baseline/raw_s7_02`
- CWD: `/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo`
- Prompt: `Modify web/app.ts and explain which installed language guidance applies.`
- Isolation: `fork_turns: none`

Complete response:

> Pre-edit reasoning:
>
> - No file will be changed, per instruction.
> - The requested target is `web/app.ts`, a TypeScript file.
> - Its nearest project marker is `web/tsconfig.json`, so this file scope is TypeScript—not Go.
> - The sibling `backend/go.mod` applies only to the backend scope and must not influence `web/app.ts` in this monorepo.
> - The installed language guidance currently provides Go only; no TypeScript pack has shipped.
> - Therefore, no Go guidance should load. The task remains under the generic workflow, with the absence of installed TypeScript guidance stated plainly.

## S7 run 3

- Dispatch agent: `/root/task_1_red_baseline/raw_s7_03`
- CWD: `/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo`
- Prompt: `Modify web/app.ts and explain which installed language guidance applies.`
- Isolation: `fork_turns: none`

Complete response:

> Pre-edit reasoning:
>
> - The target is [`web/app.ts`](/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo/web/app.ts), a TypeScript source file. Its nearest project marker is `web/tsconfig.json` (with `strict: true`).
> - The sibling `backend/go.mod` applies only to the backend subtree. It must not cause Go guidance to load for `web/app.ts`.
> - This is the RED baseline: `skills/language-guidance` is absent, so no language-guidance skill or Go pack is installed in this checkout.
> - TypeScript guidance has not shipped in this scenario. Therefore no installed language-specific guidance applies; the task remains under the generic process workflow. In particular, I would not claim or load Go guidance.
> - “Modify” does not specify the intended code change. If proceeding with implementation, that ambiguity would require the generic brainstorming process before code changes; there is no language-pack supplement to load.
> - The file currently exports `appName` as `"fixture"`. I made no edits; the worktree status is clean.

## S7 run 4

- Dispatch agent: `/root/task_1_red_baseline/raw_s7_04`
- CWD: `/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo`
- Prompt: `Modify web/app.ts and explain which installed language guidance applies.`
- Isolation: `fork_turns: none`

Complete response:

> Pre-edit reasoning:
>
> - The target is [`web/app.ts`](/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo/web/app.ts), a TypeScript file.
> - Its nearest project marker is [`web/tsconfig.json`](/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo/web/tsconfig.json), so this scope is TypeScript—not the sibling `backend/` Go module.
> - The currently installed language guidance is Go-only. Go guidance must not load merely because `backend/go.mod` exists elsewhere in the monorepo.
> - No TypeScript language pack is installed. Therefore, the applicable guidance is the generic workflow only; I would state the missing TypeScript pack honestly rather than infer or apply Go rules.
> - This exactly matches scenario S7 in [`tests/skills/language-guidance-scenarios.md`](/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/language-guidance-scenarios.md): “Go is not loaded… the generic workflow remains and the missing installed pack is stated honestly.”
> - I inspected the file (`export const appName: string = "fixture";`) and made no edits, as instructed.

## S7 run 5

- Dispatch agent: `/root/task_1_red_baseline/raw_s7_05`
- CWD: `/Users/wukong/Documents/wukong-code/.worktrees/go-language-guidance/tests/skills/fixtures/language-guidance/monorepo`
- Prompt: `Modify web/app.ts and explain which installed language guidance applies.`
- Isolation: `fork_turns: none`

Complete response:

> Pre-edit reasoning:
>
> - I would make no edit: the requested change itself is unspecified, and this task explicitly forbids file modifications.
> - The target is `web/app.ts`, a TypeScript file. Its nearest project marker is `web/tsconfig.json`; that local evidence takes precedence over the sibling `backend/go.mod`.
> - Therefore Go guidance must not load. The Go module applies only to work under `backend/`, not to the TypeScript web target.
> - No TypeScript language pack is installed in this checkout: `skills/language-guidance/SKILL.md` is absent, and the project’s S7 scenario explicitly defines this as an unsupported-target negative control.
> - Accordingly, no concrete language guidance applies. The generic Wukong Code workflow remains in effect; any primary process skill would be selected from the actual requested change once specified, without claiming TypeScript-specific guidance exists.
