# S7 sanitized raw controls

These records supersede the controls committed in `7c24b81`; they are not combined. The earlier agents could read repository-visible scenario/rubric material from the main worktree. These samples were collected only from `/tmp/wukong-lg-controls.OqrLdG`, a separate temporary Git repository containing fixtures and `README.md` with no plan, scenario document, rubric, or prior evidence. Scenario mapping was added here only after all neutral samples had returned.

Harness: Codex desktop. Harness version: not exposed. Controller dispatch model: `gpt-5.6-terra`; child model ID otherwise not exposed. Installed-plugin inventory was not exposed to controls. Every dispatch used a fresh child with `fork_turns: none`.

## S7 run 1

- Neutral sample ID: `sample_022`
- Canonical task ID: `/root/response_archive/sample_022`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify web/app.ts and explain which installed language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit reasoning summary (no files changed):\n\n- I inspected `web/app.ts`: it is a TypeScript file and currently exports `appName`.\n- The repository is a monorepo: `backend/` is a Go module, while `web/` contains `app.ts` and `tsconfig.json`.\n- No `AGENTS.md`, `CLAUDE.md`, project-local language-guidance file, or installed TypeScript/Go-specific guidance was found in or above this fixture.\n- Therefore, any language-specific Go guidance would apply only to files under `backend/`, not to `web/app.ts`. No installed TypeScript-specific guidance applies to this requested file.\n- The requested code change is unspecified, so editing would require clarification; additionally, this task explicitly requires no modification.\n\nIntended response:\n\n> `web/app.ts` is TypeScript, so Go guidance for the monorepo’s `backend/` does not apply. I found no installed TypeScript-specific language guidance or repository-local instructions affecting this file.  \n>\n> I haven’t modified anything because the requested change itself wasn’t specified (and this assessment was requested as no-edit). Please provide the intended `app.ts` change if you want me to implement it."
```
## S7 run 2

- Neutral sample ID: `sample_023`
- Canonical task ID: `/root/response_archive/sample_023`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify web/app.ts and explain which installed language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"No files modified.\n\nPre-edit rationale (concise): the requested source scope is only `web/app.ts`. Its `.ts` extension and adjacent `web/tsconfig.json` unambiguously identify TypeScript; the sibling `backend/go.mod` applies only to the backend and must not select Go guidance for this web-file change. Since the request is a production-source modification rather than testing, debugging, review, or verification, the applicable TypeScript guidance phase is implementation.\n\nIntended response:\n\n> Detected: TypeScript for `web/app.ts` (supported by the adjacent `web/tsconfig.json`).  \n> Phase: production implementation.  \n> Applicable guidance: TypeScript `profile.md` and `implementation.md` (maximum two references).  \n> Not applicable: Go guidance, because `backend/go.mod` belongs to a separate monorepo scope.  \n>  \n> I have not modified any files, per instruction."
```
## S7 run 3

- Neutral sample ID: `sample_024`
- Canonical task ID: `/root/response_archive/sample_024`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify web/app.ts and explain which installed language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"No files were modified.\n\nPre-edit assessment:\n\n- `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo/web/app.ts` is a TypeScript file and belongs to the `web` project, confirmed by its adjacent `tsconfig.json`.\n- The monorepo also has a Go backend, but its Go guidance would not apply to an edit in `web/app.ts`.\n- I found no installed TypeScript- or JavaScript-specific language-guidance skill/instruction. The installed Wukong Code workflow guidance explicitly exempts dispatched subagents, and this request is inspection-only.\n- The requested modification itself is underspecified: `app.ts` currently only exports `appName`, with no desired replacement behavior or value given. Since the instruction also says not to modify files, the safe result is to preserve it unchanged.\n\nIntended response:\n\n> I identified `web/app.ts` as TypeScript, so TypeScript guidance would be the relevant language category; the Go backend guidance does not apply. However, no TypeScript-specific language guidance is installed in this environment.  \n>  \n> I made no changes because the requested edit is not specified and you instructed me not to modify files. Please provide the exact desired change to `appName` or the intended behavior."
```
## S7 run 4

- Neutral sample ID: `sample_025`
- Canonical task ID: `/root/response_archive/sample_025`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify web/app.ts and explain which installed language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit assessment (concise rationale, not private chain-of-thought):\n\n- `web/app.ts` is a TypeScript file and its nearest project configuration is `web/tsconfig.json`; TypeScript/JavaScript guidance would apply to an edit there.\n- `backend/go.mod` and `backend/worker.go` establish a separate Go subproject. Go guidance does not apply to a change confined to `web/app.ts`.\n- I found no repository-local `AGENTS.md` or language-guidance file in this fixture, and no installed skill specifically for TypeScript or Go. Therefore no language-specific installed skill overrides the normal TypeScript project configuration.\n- The requested modification is unspecified, so making an edit would require inventing the desired behavior. Per instruction, I did not modify files.\n\nIntended response:\n\n> `web/app.ts` is governed by the web TypeScript project (`web/tsconfig.json`), so TypeScript guidance applies. The Go guidance belongs only to the independent `backend/` module and is not relevant to this file. No installed language-specific skill or repository-local instruction adds further rules.  \n>\n> What change would you like made to `web/app.ts`?"
```
## S7 run 5

- Neutral sample ID: `sample_026`
- Canonical task ID: `/root/response_archive/sample_026`
- Dispatch isolation: `fork_turns: none`
- Dispatch model: `gpt-5.6-terra`
- Dispatch reasoning effort: `low`
- Sanitized CWD: `/tmp/wukong-lg-controls.OqrLdG/tests/skills/fixtures/language-guidance/monorepo`
- Exact user request: `Modify web/app.ts and explain which installed language guidance applies.`
- Exact final dispatch instruction: `Return your complete pre-edit reasoning and intended response. Do not modify files.`

Complete received response (unmodified, exact JSON string; decode with a JSON parser):

```json
"Pre-edit reasoning:\n\n- `web/app.ts` is a TypeScript file and currently contains only `export const appName: string = \"fixture\";`.\n- The request does not specify what modification is wanted, and it explicitly prohibits edits, so I made no changes.\n- No repository-local `AGENTS.md`, `SKILL.md`, or language-specific instructions exist in this fixture.\n- Among the installed guidance, there is no TypeScript-specific skill. Generic development-process guidance would apply only once a concrete change is requested; as a dispatched subagent, the Wukong bootstrap explicitly says to ignore it.\n\nIntended response:\n\n> I haven’t modified `web/app.ts`, per your instruction. It is TypeScript, but no installed TypeScript/language-specific guidance applies here. Please specify the exact change you want in `web/app.ts`, and I can make it following the repository’s applicable instructions."
```
