I’ll first inspect the repository and its Rust-specific guidance, then I’ll outline a standard-library-only concurrency design without changing any files. Constraints I’ll preserve: output ordering, joining all started workers, and deterministic lowest-index error selection.

I’m using the workflow and Rust language guidance because this is a behavior-changing source request, even though you’ve explicitly paused before edits. I’ll keep this turn to inspection and the requested pre-edit plan.

Detected: Rust (`src/lib.rs`, `tests/batch.rs`, and nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

My intended implementation is:

- Use `std::thread::scope`, which is available under this crate’s declared Rust 1.63 MSRV and lets workers borrow `processor` and `inputs` safely—no dependency or `'static` conversion required.
- Spawn one scoped worker per input, returning `(input_index, Result<String, P::Error>)`.
- Join every handle before returning, regardless of any worker failure.
- Store successful strings by their original index, then assemble the final `Vec<String>` in input order.
- Inspect joined results in input-index order and return the first error found, guaranteeing the lowest-index error rather than the first worker to finish.
- Add the required thread-safety bounds: `P: Sync` and `P::Error: Send`.
- Preserve panic propagation only after all started workers have been joined, so a panic never leaves scoped workers unjoined.

The existing tests already cover ordered success and lowest-index failure. I would add a timing/synchronization-based test only if the project’s test style permits it, to prove completion order cannot affect the selected error.
