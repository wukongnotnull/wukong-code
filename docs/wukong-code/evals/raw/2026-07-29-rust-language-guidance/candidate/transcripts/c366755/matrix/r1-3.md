I’ll inspect the project and its Rust guidance first, then map the required concurrency/error behavior without changing any files. The constraints are: standard library only, preserve successful-result ordering, join every worker, and deterministically report the lowest-index failure.

I’m using the project’s brainstorming and Rust language guidance to keep the proposed change aligned with the existing API and toolchain before recommending an implementation.

Detected: Rust (`src/lib.rs`, `tests/batch.rs`, and nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

Intended approach, pending your approval:

- Use `std::thread::scope` (available with the project’s Rust 1.63 MSRV) to start one worker per input without adding dependencies or requiring `'static` inputs.
- Add the necessary API bounds: `P: Sync` and `P::Error: Send`, since `&P` is shared across threads and errors return from workers.
- Start every worker before collecting results; retain handles in input order.
- Join every started worker, then inspect completed results by input index. This preserves output order when all succeed and selects the first error by input index, not completion timing.
- Preserve panics rather than inventing a `P::Error` for them, while ensuring all worker handles are joined before propagating a panic.
- Add/adjust tests using only standard-library synchronization to prove concurrent start, ordered successes, and deterministic lowest-index error selection.

Constraint note: returning `Result<Vec<String>, P::Error>` cannot represent a worker panic as `P::Error`; the appropriate standard-library behavior is to rethrow it after joining all workers.
