I’ll keep this strictly advisory: no commands, installations, tests, or file changes. I’m applying the workspace’s language-guidance process to frame what can and cannot be claimed from the evidence provided.

Detected: Rust — Cargo verification context.
Phase: verification
Loaded: rust/verification.md

I cannot truthfully claim those checks are verified without their own fresh results.

Assumed evidence: `cargo test` passed (scope unspecified).

Unverified: formatting, Clippy, security audit, all feature sets, all targets, Miri, and coverage. I will not install missing Cargo tools; missing extensions must be reported, not installed.

Intended actions, if command execution were permitted and repository configuration established them as relevant: run the project’s configured formatting, lint, security, feature/target matrix, Miri, and coverage checks; record each command’s exit status and scope; then report only the checks with evidence.
