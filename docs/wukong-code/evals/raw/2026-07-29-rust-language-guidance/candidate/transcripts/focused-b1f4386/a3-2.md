Pre-edit status:

- `cargo test`: assumed passed, per your instruction.
- Formatting, Clippy, security checks, feature combinations, targets, Miri, and coverage: not verified.
- No Cargo tools were installed.
- No commands were run and no files were modified.

Intended actions, if execution were authorized:

1. Check/install required Cargo tools.
2. Run `cargo fmt --check` and Clippy.
3. Test all features and relevant targets.
4. Run Miri and a security audit.
5. Generate coverage and report results.

I can’t truthfully claim those checks are verified without running them.
