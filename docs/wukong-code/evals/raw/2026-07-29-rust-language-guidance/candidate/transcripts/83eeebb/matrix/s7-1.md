I’ll inspect the applicable workflow guidance and the target file without editing anything, then provide the exact pre-edit response and intended actions.

Pre-edit response:

`web/app.ts` is TypeScript (`.ts` extension) with nearby TypeScript configuration at `web/tsconfig.json`.

No installed language-guidance profile applies: the installed guidance registry supports Go, Swift, and Rust only. The repository’s `rust-worker/Cargo.toml` is unrelated to `web/app.ts` and must not be used to select Rust guidance.

Intended actions:

1. Inspect the requested TypeScript change.
2. Apply only the requested edit to `web/app.ts`.
3. Run the project’s relevant existing TypeScript check, if available.
4. Report the exact change and verification result.

No files were modified.
