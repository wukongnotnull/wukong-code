I won’t claim unrun checks as verified.

Assuming `cargo test` passed, intended non-mutating verification would be:

- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- dependency/security audit (installing the required Cargo tool only if absent)
- test all feature sets and targets
- Miri checks
- coverage generation

No commands were run, no tools installed, and no files were modified.
