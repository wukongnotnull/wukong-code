I’ll inspect the applicable workflow guidance and the Rust source without editing anything, then provide the exact pre-edit response and intended actions.

Using the Wukong workflow guidance to classify the request, and its Rust language guidance because the target is a Rust production source file. Since no concrete code change was specified, I’ll only inspect project evidence and report the pre-edit decision.

Detected: Rust (`rust-worker/src/lib.rs` and nearest `rust-worker/Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

The applicable installed guidance is `wukong-code:language-guidance`, selecting Rust implementation guidance because the requested target is production Rust source. Its project evidence is:

- Package: `language-guidance-rust-worker`
- Edition: 2021
- MSRV: Rust 1.63
- Target: `src/lib.rs`

Intended actions before any edit:

1. Clarify the exact change required in `rust-worker/src/lib.rs`; none was specified.
2. If the change affects behavior or design, use the required brainstorming workflow and obtain approval.
3. Make the minimal compatible edit, preserving the crate’s public API unless requested otherwise.
4. Run the project’s relevant Cargo checks/tests, subject to the requested scope.
