I’m using the language-guidance workflow to inspect the Rust project and state the required completion checks only; I will not edit any files.

Detected: Rust (explicit request and `Cargo.toml`)
Phase: verification
Loaded: rust/verification.md

Before claiming a Rust change complete, I would run:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

I would report each command’s exit code and test results, plus any skipped or unverified configurations. No files were modified, and I did not run these checks.
