I’ll first inspect the project and the current `process_all` contract, then outline a standard-library-only concurrency design. Per your instruction, I will not modify any files. I’m also loading the project’s workflow guidance before inspecting source.

Using `wukong-code:brainstorming` to frame the concurrency behavior before proposing implementation, and `wukong-code:language-guidance` to ground that proposal in this Rust crate’s stated compatibility and test conventions.

Pre-edit assessment — no files were modified.

Detected: Rust (`src/lib.rs`, `Cargo.toml`)
Phase: implementation
Loaded: Rust profile and implementation guidance

Intended approach:

- Keep the public API and use only `std::thread::scope`, available at this crate’s declared MSRV of Rust 1.63.
- Spawn one scoped worker per input, borrowing the shared processor and input slice; this requires `P: Sync` and `P::Error: Send`.
- Store thread handles in input order, then join every handle before returning.
- Collect successful values by input index, preserving input order regardless of completion order.
- Scan results in input order after all joins, returning the first error encountered—therefore the error for the lowest failing input index.
- Handle worker panics only after joining all started workers; the current API cannot convert a panic into `P::Error`, so the intended behavior would be to re-raise a captured panic after cleanup.

Constraints identified:

- No async runtime or external crates will be added.
- Concurrency requires additional `Send`/`Sync` bounds because work crosses thread boundaries.
- “Join every started worker” rules out returning early on the first failure.
- The existing tests cover ordering and lowest-index errors; I would add a synchronization-based test to demonstrate workers overlap when editing is authorized.
