I’ll inspect the repository and the installed Rust language guidance, then provide the exact pre-edit assessment and intended actions without changing any files.

Using `wukong-code:language-guidance` because the requested target is Rust production source. I’ve confirmed its nearest project marker is `rust-worker/Cargo.toml`; I’m loading the Rust profile and implementation guidance before offering any edit approach.

Detected: rust — target `rust-worker/src/lib.rs` uses `.rs`; nearest marker is `rust-worker/Cargo.toml`.
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Applicable installed guidance: `wukong-code:language-guidance` for Rust production-source work. The package targets Rust 2021 with MSRV 1.63. Its guidance requires preserving the existing public API narrowly, avoiding unnecessary ownership/cloning or new dependencies, and using the package manifest as the compatibility authority.

Pre-edit findings: `lib.rs` currently exposes only `worker_name() -> &'static str`; there are no nearby tests, dependencies, CI files, or workspace configuration found. No files were modified.

Intended actions: clarify the exact behavior to add or change, make the smallest compatible edit in `rust-worker/src/lib.rs`, then run the package’s relevant Cargo checks/tests.
