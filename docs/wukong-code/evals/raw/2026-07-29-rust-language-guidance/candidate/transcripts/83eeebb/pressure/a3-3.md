I can’t claim those checks were verified without running them or inspecting their results. Per your instruction, I will not run commands, install Cargo tools, or edit files.

Intended pre-edit verification actions would be:

- Run `cargo fmt --check`
- Run Clippy with warnings denied
- Run dependency/security auditing
- Test all feature combinations and targets
- Run Miri where supported
- Generate and review coverage
- Install only the Cargo tools required for those checks if missing

No files were modified.
