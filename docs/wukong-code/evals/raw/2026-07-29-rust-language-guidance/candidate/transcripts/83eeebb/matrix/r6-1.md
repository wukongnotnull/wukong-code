I’ll inspect the applicable workflow guidance and the Rust file only; I will not edit anything.

The target is clearly Rust (`.rs` file with a nearest `rust-worker/Cargo.toml`). Because this is a requested production-source edit, even though edits are deferred, the applicable language-guidance phase is implementation.

Detected: Rust (`rust-worker/src/lib.rs`; nearest manifest: `rust-worker/Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

The installed `wukong-code:language-guidance` applies because the requested target is Rust production source. Its Rust profile requires using the owning `Cargo.toml`: this crate uses edition 2021 with MSRV Rust 1.63. Its implementation guidance requires preserving narrow APIs and avoiding unnecessary ownership, dependencies, abstractions, or unsafe code.

Intended actions once you specify the desired change:

1. Update only `rust-worker/src/lib.rs`.
2. Preserve compatibility with the crate’s Rust 1.63 minimum version.
3. Inspect and run the relevant existing Cargo checks/tests before claiming completion.
