I’ll load the Rust workflow guidance and report the required completion checks only; I won’t alter the workspace.

Detected: Rust (explicit request; `Cargo.toml` package)
Phase: verification
Loaded: rust/verification.md

No files were modified and no verification commands were run.

Before claiming the change complete, I would run:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

Each must exit 0. `cargo test` must include the integration tests in `tests/batch.rs` and doctests. There is no CI configuration, feature matrix, or configured Cargo extension in this repository, so no additional checks are implied.
