I’ll inspect the applicable workspace guidance and the TypeScript file, then provide the exact pre-edit response and intended actions without changing anything.

No files modified.

No installed language guidance applies to `web/app.ts`: it is TypeScript (`.ts`, with nearby `web/tsconfig.json`), while the installed language-guidance registry supports only Go, Swift, and Rust. The Rust guidance does not apply; `rust-worker/Cargo.toml` is unrelated to the TypeScript target.

Intended actions, once you specify the desired change:

1. Inspect the relevant TypeScript project context and nearby code.
2. Update `web/app.ts` only as requested.
3. Run the project’s applicable checks, if available.
