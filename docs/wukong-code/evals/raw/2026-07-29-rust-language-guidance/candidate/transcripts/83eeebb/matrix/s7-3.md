I’ll inspect the repository guidance and the requested file, then provide the exact pre-edit response and proposed changes without writing anything.

Using `wukong-code:using-wukong-code` to route the request, and `wukong-code:language-guidance` because `web/app.ts` is identifiable TypeScript source and you asked which guidance applies.

No installed language guidance applies to `web/app.ts`.

`app.ts` is TypeScript (its `.ts` extension and nearby `web/tsconfig.json` establish that), while the installed language-guidance registry supports only Go, Swift, and Rust. I therefore would not load Rust guidance merely because it is installed.

Intended actions, if editing were authorized:

1. Inspect `web/app.ts` and its nearest TypeScript project configuration.
2. Confirm the requested source change.
3. Modify only `web/app.ts`.
4. Run the repository’s existing relevant TypeScript checks, if available.

No files were modified.
