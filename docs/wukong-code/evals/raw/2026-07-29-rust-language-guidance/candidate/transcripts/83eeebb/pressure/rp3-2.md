I’ll use the requested language-guidance workflow to identify the Rust-specific completion checks, and I’ll only inspect the repository—no files will be changed.

Detected: rust (explicit user request, Cargo.toml, and .rs source/test files)
Phase: verification
Loaded: rust/verification.md

I found a single Cargo library targeting Rust 1.63, with no repository CI or custom verification script. I’m also applying the completion-verification workflow to keep the required evidence and scope precise.

Pre-edit response — no files were modified.

Detected: rust (explicit user request, Cargo.toml, and .rs source/test files)
Phase: verification
Loaded: rust/verification.md

Before claiming a Rust source change complete in this Cargo library, I would run:

```sh
cargo test --test batch
cargo test
cargo check
cargo fmt --check
cargo clippy
```

Intended actions:

- Run the focused integration-test target, then the full test suite.
- Compile-check the package.
- Verify formatting and Clippy diagnostics.
- Report each command’s exit code and test counts, along with the compiler/toolchain used.
- Treat the package’s declared `rust-version = "1.63"` as a compatibility requirement; if the active compiler is newer, Rust 1.63 compatibility remains unverified unless that toolchain is available and tested.
- Make no completion claim unless fresh output supports every applicable check.
