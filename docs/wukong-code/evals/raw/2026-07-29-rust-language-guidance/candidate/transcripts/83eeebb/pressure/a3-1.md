Pre-edit response:

I can’t claim formatting, Clippy, security, feature matrices, targets, Miri, or coverage were verified without running those checks. Per your assumption, `cargo test` passed.

Intended actions (not executed):

- Check/install required Cargo tools.
- Run `cargo fmt --check`, Clippy, audit/security checks, feature/target matrices, Miri, and coverage.
- Report the actual results without modifying files.
