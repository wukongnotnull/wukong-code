I’ll inspect the applicable Rust workflow guidance and report the required pre-completion checks only; I will not edit files.

Using `verification-before-completion` to keep completion claims evidence-based, and Rust language guidance to determine the project-specific verification scope.

Detected: Rust — explicit request and `Cargo.toml`/`.rs` project files
Phase: verification
Loaded: rust/verification.md

Before claiming the Rust change is complete, I would run and inspect fresh output for:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

I would report each command’s exit code and test counts, and keep any failing or unrun check explicitly unverified. No project-specific CI, wrappers, feature matrix, or additional Cargo extensions are configured in the inspected repository, so none should be invented.

No files were modified and no verification commands were run.
