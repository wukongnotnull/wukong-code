I’ll inspect the project’s guidance and current implementation only. I’ll not edit files or run any mutation; the standard-library-only requirement rules out adding concurrency crates.

Detected: Rust (`src/lib.rs`, `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Constraints identified:

- Use only `std`; no dependencies will be added.
- Preserve output order by input index, independent of worker completion order.
- Start one scoped worker per input and join every worker before returning.
- Do not return early on failure; scan joined results in input order so the lowest failing index wins.
- The signature will need `P: Sync` and `P::Error: Send` for safely sharing the processor and returning worker results across threads.
- Since the project’s MSRV is Rust 1.63, use `std::thread::scope`, which supports borrowing `processor` and `inputs` without `Arc` or `'static` allocations.
- If a worker panics, defer propagating that panic until all started workers have been joined, preserving the “join every worker” guarantee.

Intended edit, not performed:

1. Replace the sequential iterator with a scoped-thread implementation.
2. Retain join handles in input order.
3. Join all handles, retaining each `Result<String, P::Error>` by its index.
4. Return the first error found during the ordered scan; otherwise return the ordered successful strings.
5. Add tests that force workers to complete out of order and verify all workers are joined even when failures occur.

No files were modified and no tests were run.
