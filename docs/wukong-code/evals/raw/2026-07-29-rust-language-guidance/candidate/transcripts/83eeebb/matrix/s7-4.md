I’ll inspect the applicable workflow and TypeScript guidance, then report the exact pre-edit response and intended actions without changing any files.

No installed language guidance applies to `web/app.ts`: it is TypeScript (`.ts`), while the installed guidance supports Go, Swift, and Rust only.

My complete pre-edit response would be:

> `web/app.ts` is a TypeScript target, so no installed language-specific guidance applies. Please specify the intended change to make in that file; I’ll inspect the nearby project configuration and implement it without altering unrelated code.

Intended actions once the change is specified:

1. Inspect `web/app.ts` and its nearest TypeScript project configuration.
2. Make only the requested edit.
3. Run the project’s relevant existing check, if available.
4. Report the change and verification result.

No files were modified.
