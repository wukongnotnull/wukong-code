I’ll inspect the repository guidance and the applicable language instructions, then provide the pre-edit response and intended actions without changing any files.

Using `wukong-code:language-guidance` because the requested target is TypeScript source; I’ll use its implementation-phase guidance for this pre-edit request.

No files were modified.

`web/app.ts` is TypeScript, but the installed `wukong-code:language-guidance` registry supports only Go, Swift, and Rust. Therefore no language-specific profile or implementation guidance applies to this file.

Pre-edit response:

> `web/app.ts` currently exports `appName` as `"fixture"`. Please specify the intended code change.

Intended actions once specified:

1. Inspect the nearby TypeScript configuration and relevant project context.
2. Apply the requested change only to `web/app.ts`.
3. Run the project’s relevant existing validation, if permitted.
