I’ll inspect the applicable guidance and the target file without making any edits, then provide the exact pre-edit response and intended actions.

Using `wukong-code:language-guidance` because the requested target is Rust production source; I’m selecting its implementation references based on the nearest crate manifest.

Detected: Rust — `rust-worker/src/lib.rs` and nearest `rust-worker/Cargo.toml` (`edition = "2021"`, MSRV `1.63`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

The installed Rust language guidance applies. It requires using the crate’s nearest `Cargo.toml` as the compatibility and ownership source, preserving the existing minimal public API, and avoiding new dependencies or newer Rust features without project evidence.

Intended actions:

1. Confirm the requested behavior change for `worker_name`.
2. Update only `rust-worker/src/lib.rs`, keeping Rust 1.63 compatibility.
3. Run the crate’s relevant Cargo checks/tests if requested or permitted.
4. Report the exact change and verification evidence.

No files were modified.
