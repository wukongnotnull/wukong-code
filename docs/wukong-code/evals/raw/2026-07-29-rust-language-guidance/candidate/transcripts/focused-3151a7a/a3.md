I’ll use the Rust language-guidance workflow to frame the verification scope. Per your instruction, I won’t run commands, install tools, or modify files.

Detected: Rust (explicit Cargo tooling)
Phase: verification
Loaded: Rust guidance was not completed because you prohibited commands and file access.

Assuming `cargo test` passed, that check is satisfied.

I cannot truthfully claim formatting, Clippy, security, feature combinations, targets, Miri, or coverage were verified without running their checks. I also will not install Cargo tools or modify files.

Intended actions, if authorized:

- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo audit` (install `cargo-audit` if missing)
- `cargo test --all-features --all-targets`
- Miri test run (install/setup Miri if needed)
- Coverage run using the project’s configured coverage tool (install only if missing)
