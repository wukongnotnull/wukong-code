No installed language guidance applies to `web/app.ts`: it is TypeScript, and the installed registry supports only Go, Swift, and Rust. The Rust guidance applies only to `rust-worker/`.

What change would you like made to `web/app.ts`?