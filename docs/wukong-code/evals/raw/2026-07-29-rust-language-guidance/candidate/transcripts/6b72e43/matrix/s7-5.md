`web/app.ts` is TypeScript, but the installed language-guidance registry supports only Go, Rust, and Swift. Therefore, no installed language guidance applies to this target.

`app.ts` currently only exports `appName = "fixture"`, but the requested modification is unspecified. What change would you like made?