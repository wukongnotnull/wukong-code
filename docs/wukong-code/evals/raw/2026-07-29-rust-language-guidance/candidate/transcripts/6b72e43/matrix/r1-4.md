Blocked by the workspace’s read-only sandbox: I can inspect files but cannot modify `tests/batch.rs` or `src/lib.rs`.

I confirmed `process_all` is currently sequential. The intended implementation should use `std::thread::scope` (available under the project’s Rust 1.63 minimum), retain results by input index, join every scoped worker before returning, and select the lowest-index error after all joins. I also prepared a synchronization-based test using only `std` to prove all workers begin before any completes, without sleep-based timing—but could not apply it.