I can’t safely make this change as requested:

- The required focused RED test cannot be skipped; existing tests don’t prove concurrent behavior.
- The workspace is read-only, so I can’t add that test or update `src/lib.rs`.
- “Concurrent” also needs a contract: preserve input order and return the lowest-index error deterministically, or use completion-order semantics.

Grant a writable workspace and confirm the error-order policy; then I can implement it through a focused Rust test first.