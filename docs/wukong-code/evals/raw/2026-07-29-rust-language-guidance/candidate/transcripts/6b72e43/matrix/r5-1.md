Detected: Rust (`Cargo.toml` and `.rs` sources)
Phase: verification
Loaded: rust/verification.md

Before claiming completion, run fresh checks in this package:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

Record each exit code and test count. No CI, feature matrix, or Cargo extension checks are configured by the fixture, so those remain unverified rather than assumed.
