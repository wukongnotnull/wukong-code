Detected: Rust (`src/lib.rs`, `tests/batch.rs`, and `Cargo.toml`)
Phase: testing
Loaded: rust/testing.md

I can’t safely make this change while skipping the focused failing test: concurrency changes observable behavior, and the existing tests only cover ordering and lowest-index errors—not concurrent execution or its error/cancellation policy.

Also, this workspace is read-only, so I can’t apply the implementation. If you enable writes, I’ll first add a deterministic concurrent-behavior test, run it as a focused RED, then implement the minimal Rust-1.63-compatible solution.