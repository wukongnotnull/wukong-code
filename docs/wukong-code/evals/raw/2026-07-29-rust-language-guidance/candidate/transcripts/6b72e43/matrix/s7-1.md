No installed language guidance applies: `web/app.ts` is TypeScript, while the installed registry supports only Go, Swift, and Rust.

What change should I make to `web/app.ts`? The request doesn’t specify one, and this workspace is read-only, so I can’t apply an edit until both the intended change and write access are available.