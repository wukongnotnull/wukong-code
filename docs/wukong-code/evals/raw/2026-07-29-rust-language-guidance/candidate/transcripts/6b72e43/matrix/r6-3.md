Detected: Rust — `rust-worker/src/lib.rs` and its nearest `rust-worker/Cargo.toml`
Phase: implementation
Loaded: `rust/profile.md`, `rust/implementation.md`

The installed Rust guidance applies. It requires using the nearest package manifest (Rust 2021, MSRV 1.63) and preserving its existing API/error/concurrency conventions.

I need the intended change to `lib.rs`; the request only names the file, not the desired behavior. Also, this workspace is read-only, so I can inspect and propose a patch but cannot apply it here.