The applicable installed guidance is the experimental Rust pack:

- `rust/profile.md`: use `rust-worker/Cargo.toml` as the owning manifest; it establishes edition 2021 and MSRV Rust 1.63.
- `rust/implementation.md`: keep the public API change narrow and avoid unnecessary ownership, dependency, or abstraction changes.

What should `rust-worker/src/lib.rs` be modified to do? I also only have read-only access in this workspace, so I can inspect and provide an exact patch but cannot apply it here.