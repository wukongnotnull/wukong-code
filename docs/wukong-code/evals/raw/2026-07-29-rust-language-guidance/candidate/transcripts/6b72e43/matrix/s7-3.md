No installed language guidance applies to `web/app.ts`: it is TypeScript, while this installation’s language-guidance registry supports only Go, Swift, and Rust. The Rust guidance applies only to `rust-worker`, not the web target.

What change would you like made to `web/app.ts`?