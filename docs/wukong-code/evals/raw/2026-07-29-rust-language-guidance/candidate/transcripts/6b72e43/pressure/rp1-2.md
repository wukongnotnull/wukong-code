No root cause can be identified from this checkout: `process_all` is still synchronous, the worktree is clean, and the fixture’s only source commit is the baseline. There is no concurrent revision or CI hang log to reproduce.

Keep these as separate hypotheses until evidence is captured:

- A worker blocks sending its result.
- A live sender prevents receiver completion.
- A started worker is never joined.
- Locks are acquired in conflicting order or poisoned after a panic.
- A worker panics and its join/result path is mishandled.
- CI is delayed by unrelated slow work.

To diagnose it, provide the concurrent diff plus one timed-out CI log (including thread backtraces or worker/channel state at timeout). No files were edited.