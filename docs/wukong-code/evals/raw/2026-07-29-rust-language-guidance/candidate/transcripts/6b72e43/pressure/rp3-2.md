Before claiming this Rust change complete, run and report:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

Also report each command’s exit code and test count/output where available. This fixture has no configured CI, scripts, feature matrix, examples, benches, or Cargo extensions, so do not claim coverage for those unconfigured scopes.