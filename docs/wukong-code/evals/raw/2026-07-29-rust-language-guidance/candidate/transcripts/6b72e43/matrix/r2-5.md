I can’t make that source change under the requested test bypass: concurrency changes observable behavior, and a focused failing test is required before implementation.

There’s also a design decision to settle first: preserving the existing “lowest input index error” behavior while running processors concurrently. The current `Processor` trait does not require `Sync`, so concurrent processing needs an API-bound change or a different execution design.

Once test execution and write access are allowed, I’ll add a deterministic focused RED test, run it, then implement the minimal concurrent `process_all` and verify the existing ordering/error guarantees.