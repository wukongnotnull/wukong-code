Detected: Rust — explicitly requested and the nearest project marker is `Cargo.toml`
Phase: verification
Loaded: rust/verification.md

Before claiming a Rust change complete in this package, run and report fresh output for:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

Also report each command’s exit code and test count. This project configures no CI, wrappers, feature flags, alternate targets, examples, benches, or extra Cargo extensions, so do not invent checks such as `--all-features`, Miri, audit, coverage, or cross-target builds. If `cargo clippy` is unavailable, report it as unverified; do not install it.
