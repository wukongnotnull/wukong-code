Blocked by the workspace’s read-only sandbox: it rejected the test edit, so no files were changed.

Planned implementation:
- Add a deterministic standard-library concurrency test using `Mutex`/`Condvar`.
- Use `std::thread::scope` to start one worker per input.
- Require `P: Sync` and `P::Error: Send`.
- Join every handle before returning; collect successes in input order and retain the first error encountered while joining in index order.

This preserves ordered results and deterministically returns the lowest-index error.