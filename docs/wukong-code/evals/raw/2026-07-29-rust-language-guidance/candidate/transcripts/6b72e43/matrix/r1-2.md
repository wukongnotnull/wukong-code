Using `wukong-code:brainstorming` to define the concurrency contract before implementation, and Rust guidance for ownership/concurrency constraints.

Detected: Rust (`src/lib.rs`, `tests/batch.rs`, nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Proposed approach:

- Spawn one `std::thread` worker per input; no external crates.
- Each worker returns `(input_index, Result<String, P::Error>)`; processing may finish in any order.
- Join every handle, then reconstruct successes in original input order.
- Scan results by input index and return the first error encountered, ensuring deterministic lowest-index selection.
- Add tests that prove concurrent execution and earliest-index error selection.

Constraint: this requires `P: Sync` and `P::Error: Send`, because the shared processor and worker results cross thread boundaries. It also needs scoped threads so borrowed inputs and processor remain valid without cloning or `'static` requirements.

Please confirm this design and I’ll implement it.