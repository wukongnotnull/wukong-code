I can’t establish a root cause from this checkout: `process_all` is still sequential, and there’s no concurrent revision or CI hang log in reachable history.

The Rust-specific concurrency causes remain distinct until we see that change:

- blocked channel send/receive
- sender retained, so receiver never observes closure
- workers never joined or a worker panics before signaling completion
- lock-order deadlock/poisoning
- blocking work inside the executor
- unrelated slow CI work

Existing tests can’t reproduce it here; attempting `cargo test` is blocked by the read-only sandbox creating Cargo’s build lock.

Please provide the concurrent `process_all` diff/commit and one hanging CI log (especially the last emitted test/task). Then I can trace the actual wait/ownership path and identify the cause.