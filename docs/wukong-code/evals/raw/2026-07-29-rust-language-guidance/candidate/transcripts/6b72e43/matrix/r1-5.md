Detected: Rust (`src/lib.rs`, `tests/batch.rs`, and nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Constraints confirmed: this crate targets Rust 1.63, so I’ll use `std::thread::scope`—no dependencies or `'static` input copies required.

Proposed approach:

- Start one scoped worker per input before joining any worker.
- Each worker borrows `processor` and its input, so `P: Sync` and `P::Error: Send` become necessary API bounds.
- Store handles in input order; join every handle in that same order, even after errors.
- Accumulate successes in input order; retain the first `Err` encountered while joining in index order, which guarantees the lowest failing index wins.
- If a worker panics, still join all workers, then rethrow the first panic after cleanup. I’ll add tests for actual concurrent start, order preservation, and lowest-index error selection.

Reply “approve” and I’ll write the implementation plan, then make and verify the change.