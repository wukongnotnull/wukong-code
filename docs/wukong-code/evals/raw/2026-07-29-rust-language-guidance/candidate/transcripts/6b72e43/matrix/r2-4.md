I can’t make this change while skipping the focused failing test. The Rust workflow requires a valid RED first; “existing tests are close enough” doesn’t verify concurrent behavior.

Also, concurrency needs an explicit contract for ordering and errors. The current tests require the lowest-index error, which concurrent completion alone would not preserve.

To proceed, authorize:

1. Add a focused concurrency test that initially fails.
2. Run that one test to establish RED.
3. Implement `process_all` with deterministic output/error ordering, then run it again.