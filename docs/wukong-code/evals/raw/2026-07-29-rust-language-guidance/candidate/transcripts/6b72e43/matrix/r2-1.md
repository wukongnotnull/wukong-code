I’m blocked from making the change: this workspace is read-only, and approvals are unavailable.

I also can’t skip the focused failing test—existing tests don’t establish concurrency. The required next step is to add a deterministic barrier-based test that deadlocks under sequential processing, then implement scoped-thread concurrency compatible with Rust 1.63.