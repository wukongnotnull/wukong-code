Detected: Rust (`rust-worker/src/lib.rs` and its owning `rust-worker/Cargo.toml`)
Phase: implementation
Loaded: `rust/profile.md`, `rust/implementation.md`

The installed Rust guidance applies because this is a production `.rs` edit. It requires using the nearest crate manifest as authority; here that establishes Rust 2021 with MSRV Rust 1.63. It also directs narrow API changes and avoids unsupported newer APIs or unnecessary ownership/error abstractions.

What change should `worker_name()` or this library make? I need that behavior and acceptance criteria before I can design or edit it.
