The installed Rust language guidance applies: `rust/profile.md` and `rust/implementation.md`.

It’s an experimental Rust pack, selected from the `.rs` target and its nearest Cargo manifest. I need the intended code change for `rust-worker/src/lib.rs`; currently it only exposes `worker_name()`.