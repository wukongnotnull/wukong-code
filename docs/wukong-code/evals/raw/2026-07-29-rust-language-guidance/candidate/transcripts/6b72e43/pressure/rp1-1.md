I couldn’t find a concurrent Rust change in this workspace to diagnose.

Evidence:
- `src/lib.rs` is purely sequential: it iterates inputs and calls `Processor::process`; it creates no threads, channels, locks, tasks, or joins.
- The only history for this fixture is its initial addition (`1a57a6d`); there is no later concurrent revision.
- The working tree is clean.

The focused test command could not run because the read-only sandbox prevents Cargo from creating `target/debug/.cargo-build-lock`. That is an environment permission failure, not an observed hang.

With the concurrent revision or a hang trace absent, these must remain separate hypotheses: blocked sends, a sender kept alive, worker joins, lock ordering, panic paths, or unrelated slow work. Please provide the relevant diff/branch and a command or test that hangs, and I can trace it without editing files.