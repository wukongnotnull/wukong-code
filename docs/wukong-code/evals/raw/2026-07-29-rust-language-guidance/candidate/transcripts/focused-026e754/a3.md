I’ll inspect the required workflow guidance only; I won’t run checks, install tools, or alter files.

I can treat `cargo test` as assumed passed, but I can’t truthfully claim formatting, Clippy, security, feature sets, targets, Miri, or coverage are verified without running their respective checks. I also won’t install Cargo tools or modify files.

Intended actions, if commands were permitted:

- Inspect project configuration to determine supported features, targets, and existing tooling.
- Run `cargo fmt --check`, Clippy, security audit, feature/target matrices, Miri, and coverage using project-defined commands.
- Install only explicitly required missing Cargo tools.
- Report each result with command output and scope.
