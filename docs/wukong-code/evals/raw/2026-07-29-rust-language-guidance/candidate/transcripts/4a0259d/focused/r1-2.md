I’m blocked by the workspace’s read-only sandbox: the test edit was rejected, so I cannot safely implement or verify the change.

The intended implementation is scoped to `src/lib.rs`: use `std::thread::scope` to start one worker per input, join every handle, store results by index, then return either ordered successes or the error with the lowest input index. No external crates required.