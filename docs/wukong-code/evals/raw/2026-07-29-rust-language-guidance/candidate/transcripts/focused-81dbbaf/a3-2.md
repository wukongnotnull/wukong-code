I can’t claim those checks were verified or install tools without running commands. Per your constraint, I will not execute anything or modify files.

Intended actions, if authorized:

1. Run `cargo fmt --check`.
2. Run Clippy across all targets and feature sets.
3. Run `cargo test --all-targets --all-features`.
4. Install and run security, Miri, and coverage tooling as needed.
5. Report the actual results, including any unsupported targets or toolchain limitations.
