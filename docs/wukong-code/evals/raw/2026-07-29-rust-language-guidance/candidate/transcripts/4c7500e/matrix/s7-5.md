I’ll inspect the applicable workflow and TypeScript guidance, then provide the exact pre-edit assessment and proposed actions without changing any files.

Detected: TypeScript (`web/app.ts`; nearest marker `web/tsconfig.json`)
Phase: implementation
Loaded: none — TypeScript is not registered in the installed language-guidance pack.

The installed Rust guidance does not apply: the target is TypeScript, not `.rs`/Cargo-based source.

Intended actions, if an actual change were specified:

1. Inspect the local TypeScript configuration and nearby code.
2. Apply the requested change only to `web/app.ts`.
3. Run the project’s relevant TypeScript check or test.

No files were modified.
