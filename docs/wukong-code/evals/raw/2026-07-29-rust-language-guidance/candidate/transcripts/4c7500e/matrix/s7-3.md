I’ll inspect the repository guidance and `web/app.ts` without editing anything, then provide the exact pre-edit response and intended actions.

Using `wukong-code:language-guidance` because this is a requested source-file change, even though you’ve explicitly asked me to stop before the edit. The target’s `.ts` extension is the relevant evidence; the Rust worker manifest is unrelated to `web/app.ts`.

No files were modified.

Pre-edit response:

> `web/app.ts` is TypeScript, but the installed language-guidance registry supports only Go, Swift, and Rust. Therefore no installed language-specific guidance applies to this target. The nearby Rust manifest applies only to `rust-worker`, not to `web/app.ts`.
>
> Current file content:
>
> ```ts
> export const appName: string = "fixture";
> ```
>
> Please specify the intended change to `web/app.ts`.

Intended actions: apply only the requested TypeScript change after you provide it, then inspect or run the project’s available validation command if one exists.
