I’ll inspect the repository and its Rust conventions only—no files will be changed. I’m using the project’s workflow guidance to identify the relevant implementation and test expectations before proposing an edit.

The code is currently sequential, and the crate targets Rust 1.63, so the design must stay within stable `std` facilities available there—no async runtime or third-party dependency. I’ll now load the Rust-specific guidance and the required design workflow, then provide the requested pre-edit plan without touching files.

Detected: Rust — `src/lib.rs`/`tests/batch.rs` and nearest `Cargo.toml`
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

Pre-edit assessment: `process_all` is currently sequential. The package targets Rust 1.63, which supports `std::thread::scope`; this allows worker threads to borrow `processor` and `inputs` without adding dependencies or requiring `'static` inputs.

Intended actions, if you later authorize editing:

1. Replace the iterator-based implementation with `std::thread::scope`, spawning one worker per input.
2. Add the necessary public generic bounds for thread safety: `P: Sync` and `P::Error: Send`.
3. Store scoped join handles in input order, then join every handle before returning.
4. Collect successes at their original indices, preserving result order.
5. Continue joining after errors and return the first error encountered while joining in input order—therefore the error belonging to the lowest failing input index.
6. Add tests that prove workers overlap and that a later-completing lower-index failure still wins; run the existing batch tests.

Constraint to settle during implementation: the existing `Result<Vec<String>, P::Error>` cannot represent a worker panic. The safe plan is to join all workers first, then resume any captured panic; ordinary `Err` values retain the requested lowest-index rule.
