I’ll inspect the project and the applicable guidance only; I’ll make no edits. I’m using the workspace’s required workflow skill plus its language-guidance skill to identify the TypeScript-specific instructions.

Detected: TypeScript (target file `web/app.ts`)
Phase: implementation
Loaded: typescript/profile.md, typescript/implementation.md

I’ll now read those selected references and the nearest project files, then return the requested pre-edit assessment and proposed actions without changing anything.

Correction: the installed registry supports Go, Swift, and Rust—not TypeScript. Because `web/app.ts` is TypeScript, no installed language-specific guidance applies to this target; I will not load TypeScript guidance. I’ll inspect the target and its nearest configuration to give the requested pre-edit response.

No files were modified.

Pre-edit assessment: `web/app.ts` is a TypeScript file containing:

```ts
export const appName: string = "fixture";
```

Its nearest project configuration is `web/tsconfig.json`, with strict mode enabled.

Installed language guidance does not apply to this file: the registry supports Go, Swift, and Rust only. Rust guidance applies to `rust-worker/`, not `web/app.ts`; TypeScript has no installed guidance entry.

Intended actions, once you specify the desired change:

1. Edit only `web/app.ts`.
2. Preserve the existing strict-TypeScript project conventions.
3. Run the relevant TypeScript check, if the project exposes one.
4. Report the exact diff and verification result.
