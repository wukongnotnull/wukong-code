I’ll keep this read-only. I’m loading the required workflow guidance to give the exact pre-edit completion checks for this Rust task.

Detected: Rust — explicit request and nearest project marker `Cargo.toml`
Phase: verification
Loaded: rust/profile.md, rust/verification.md

Pre-edit response and intended actions (read-only):

1. Run the affected integration tests:

   ```sh
   cargo test --test batch
   ```

2. Run the complete package test suite, including unit, integration, and doctests:

   ```sh
   cargo test
   ```

3. Confirm the Rust 1.63-compatible crate compiles:

   ```sh
   cargo check
   ```

4. Verify formatting:

   ```sh
   cargo fmt --check
   ```

5. Run the configured/default Rust lints:

   ```sh
   cargo clippy
   ```

Before claiming completion, I would inspect each command’s full output and exit code, report test counts, and state any skipped coverage explicitly. The package declares Rust 2021 with MSRV `1.63`; no CI workflow, custom script, feature matrix, or extra Cargo extension is present, so no additional checks are evidenced.
