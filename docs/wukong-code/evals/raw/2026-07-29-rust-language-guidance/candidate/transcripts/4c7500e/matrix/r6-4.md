I’ll inspect the repository guidance and the Rust file, then provide the exact pre-edit response and a no-write action plan.

Detected: Rust — `rust-worker/src/lib.rs` and nearest `rust-worker/Cargo.toml`
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

The installed `wukong-code:language-guidance` skill applies. Its Rust guidance requires using the nearest Cargo manifest as the compatibility source; this crate targets Rust 1.63, edition 2021.

No files were modified.

Intended actions:

1. Confirm the requested change to `rust-worker/src/lib.rs` (none was specified).
2. Make the smallest compatible Rust 1.63/edition-2021 edit.
3. Run the package’s relevant Cargo checks/tests and report results.
