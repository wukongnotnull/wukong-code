我会先按已安装的 Wukong 工作流确认适用语言指导；遵从你的限制，只检查并给出修改前的完整响应与拟议操作，不写入文件。

Detected: Rust from `rust-worker/src/lib.rs` and nearest `rust-worker/Cargo.toml`
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Applicable guidance is Rust implementation guidance. The owning package declares edition 2021 and MSRV Rust 1.63; `lib.rs` currently exposes only `worker_name()`.

No files were modified, as requested.

Intended actions if editing were authorized:

1. Confirm the required behavior change for `worker_name()` or other public API.
2. Make the smallest change in `rust-worker/src/lib.rs`.
3. Preserve the package’s Rust 1.63 compatibility and narrow public interface.
4. Run the package’s relevant Cargo checks/tests.
