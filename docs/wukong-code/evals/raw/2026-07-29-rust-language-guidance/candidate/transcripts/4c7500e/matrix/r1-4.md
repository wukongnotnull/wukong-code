I’ll first inspect the project and its Rust guidance, then I’ll give a complete pre-edit plan only. I will not modify files or run any write-capable actions.

I’m using the brainstorming workflow because this changes behavior, and Rust language guidance to make the pre-edit plan match the crate’s declared constraints.

Detected: Rust (`src/lib.rs` and nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

Current `process_all` processes inputs sequentially via iterator collection. The crate uses Rust 2021 with MSRV Rust 1.63 and no dependencies, so the standard-library solution is `std::thread::scope`.

Intended actions, if you approve editing:

- Spawn one scoped worker per input, allowing workers to borrow `processor` and the input slice without `'static` allocations.
- Add the necessary public-function bounds: `P: Sync` and `P::Error: Send`, because processor references and errors cross thread boundaries.
- Store each worker handle at its input index; join every started worker, even after observing an error.
- Collect joined results by input index, preserving successful output order.
- Return the first error encountered while joining in input-index order, guaranteeing the lowest failing index regardless of completion order.
- Define worker panic behavior explicitly in code: after joining all workers, resume a captured panic (the existing `Result<_, P::Error>` cannot represent a panic).
- Update/add tests to prove concurrent completion does not affect success ordering or lowest-index error selection, then run the crate’s test suite.

No files were modified. Please approve this design before any edit.
