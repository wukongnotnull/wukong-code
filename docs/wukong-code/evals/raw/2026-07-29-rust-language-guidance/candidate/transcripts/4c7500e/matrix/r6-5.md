I’ll inspect the applicable workflow guidance and the Rust source without editing anything, then provide the requested pre-edit response and intended actions.

Detected: Rust — explicit `rust-worker/src/lib.rs` target and its `.rs` extension; nearest marker is `rust-worker/Cargo.toml`.
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

The installed Rust language guidance applies. The package targets Rust 2021 with MSRV `1.63`; its nearest `Cargo.toml` is authoritative.

Intended actions, if you provide the desired code change:

1. Preserve the package’s existing public API and Rust 1.63 compatibility.
2. Make only the scoped change in `rust-worker/src/lib.rs`.
3. Inspect nearby conventions and run the package’s relevant Cargo checks/tests.

No files were modified.
