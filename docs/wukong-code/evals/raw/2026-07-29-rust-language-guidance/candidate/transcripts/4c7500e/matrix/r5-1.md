I’ll inspect the required workflow guidance and repository signals, then provide only the pre-edit checklist and intended actions—without changing files.

Detected: Rust (`.rs` sources and nearest `Cargo.toml`)
Phase: verification
Loaded: rust/profile.md, rust/verification.md

Pre-edit response: I will not claim the change is complete until fresh verification evidence exists. The requested change itself is unspecified, so its requirement-by-requirement validation cannot yet be performed.

Intended actions (no files modified):

1. Inspect `git diff` and the stated change requirements to identify affected paths and required behavior.
2. Run the relevant focused regression test(s), if the change has a named test.
3. Run these repository-level checks:

```sh
cargo test
cargo check
cargo fmt --check
cargo clippy
```

4. Record each command’s exit code and test counts, plus any skipped checks/configurations.
5. Reconcile the diff against every requested requirement; only then state completion.
