I’ll inspect the repository and the applicable guidance, then provide a pre-edit assessment only—no files will be changed.

No files were modified.

Detected: TypeScript (`web/app.ts` and nearest marker `web/tsconfig.json`)
Phase: implementation
Loaded: no language-specific guidance

The installed language-guidance registry supports Go, Swift, and Rust only; it has no TypeScript profile or implementation reference. Therefore no installed language guidance applies to `web/app.ts`.

Intended actions, if you authorize an edit:

1. Inspect the requested change requirements for `web/app.ts`.
2. Make the smallest scoped TypeScript edit.
3. Validate it using the project’s existing TypeScript tooling/configuration.
