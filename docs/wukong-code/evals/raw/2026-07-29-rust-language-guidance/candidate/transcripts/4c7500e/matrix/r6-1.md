I’ll inspect the repository and the relevant Rust guidance only; I will not modify any files.

Detected: rust from `rust-worker/src/lib.rs` and nearest `rust-worker/Cargo.toml`
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

The installed `wukong-code:language-guidance` skill applies because the requested target is Rust production source. Its Rust guidance requires using the owning manifest as compatibility evidence; this crate specifies edition 2021 and MSRV Rust 1.63.

Intended actions before editing:

1. Confirm the precise requested change to `worker_name` or other library API.
2. Preserve the current minimal public API and Rust 1.63 compatibility.
3. Make only the scoped change in `rust-worker/src/lib.rs`.
4. Run the package’s relevant Cargo checks/tests, if requested or available.
