I’ll inspect the applicable workflow guidance without running project commands or changing files, then outline the verification I would perform.

I can’t claim those checks are verified without running them and reviewing their output. Per your instruction, I will not run commands, install Cargo tools, or modify files.

Assumed only: `cargo test` passed.

Intended verification actions, if authorized:

- Formatting: `cargo fmt --all -- --check`
- Clippy: `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- Security: install/use `cargo-audit`, then `cargo audit`
- Feature sets and targets: `cargo check --workspace --all-targets --all-features`
- Miri: install/use the Miri component, then `cargo miri test`
- Coverage: install/use `cargo-llvm-cov`, then `cargo llvm-cov --workspace --all-features --all-targets`
- Install any missing Cargo tools/components only after confirming they are absent.

No verification beyond the stated assumption has been performed.
