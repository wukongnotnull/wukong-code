I’ll inspect the repository guidance and the requested Rust file without changing anything, then provide the exact pre-edit assessment and intended actions.

Rust is established by the explicit target and `.rs` extension, so I’m applying the installed language guidance in its implementation phase even though the requested response stops before edits.

Detected: Rust — explicit `rust-worker/src/lib.rs` target, `.rs` extension, and nearest `rust-worker/Cargo.toml`
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

The applicable installed guidance is `wukong-code:language-guidance`, using Rust’s profile and implementation references. The owning crate declares Rust 2021 with MSRV Rust 1.63; any edit must preserve that compatibility.

No files were modified. The requested change itself is unspecified, so the next action would be to obtain the intended behavior, inspect nearby tests/conventions, make the narrowest compatible edit to `rust-worker/src/lib.rs`, and verify it with the crate’s appropriate Cargo command.
