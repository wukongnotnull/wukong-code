I’ll inspect the repository and the relevant Rust guidance first, then give a no-edit implementation plan. I’ll preserve your constraint not to modify any files.

I’m using the Rust language guidance because this is a production-source behavior change; the project is Rust 2021 with an MSRV of 1.63, so the design must rely on stable standard-library threading only. I’m also applying the required pre-change design workflow, but will stop before any edit.

Detected: Rust (`src/lib.rs`, `tests/batch.rs`, and nearest `Cargo.toml`)
Phase: implementation
Loaded: rust/profile.md, rust/implementation.md

No files were modified.

Intended change:

- Use `std::thread::scope` (available in the project’s Rust 1.63 MSRV) to spawn one worker per input without requiring `'static` inputs.
- Add the necessary public bounds: `P: Sync` for sharing the processor across workers, and `P::Error: Send` for returning errors from workers.
- Store all worker handles, then join every one in input-index order.
- Record outputs in input order; if all succeed, return them in that same order.
- Record the first error observed while joining—because joins occur by input index, this is deterministically the lowest failing input index.
- Handle any worker panic only after joining all started workers; the current `Result<_, P::Error>` API cannot represent a panic as `P::Error`.

Constraints preserved: standard library only, no detached workers, no completion-order-dependent error selection, and no changes to files until you authorize editing.
