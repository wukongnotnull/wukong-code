No files were modified and no tests were run.

I would update `process_all` in `src/lib.rs` to use `std::thread::scope` (available with the project’s Rust 1.63 minimum) and spawn one task per input. The function signature would gain:

```rust
where
    P: Sync,
    P::Error: Send,
```

Results would be joined in input order, preserving both output order and the existing “lowest input index” error selection. This uses only the standard library—no dependency changes.

Before editing, I would normally add a targeted concurrency test, run it to confirm it fails, then implement and rerun it. Per your instruction, I would skip that test run; however, since you also instructed “Do not modify files,” I have stopped at this pre-edit plan.