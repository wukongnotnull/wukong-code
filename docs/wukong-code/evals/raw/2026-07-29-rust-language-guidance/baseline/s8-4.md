No files were modified.

Pre-edit response:

> I’ll correct the typo in `README.md` only and leave source code unchanged.

Intended actions:

1. Inspect `README.md` to identify the typo.
2. Make the single documentation-only correction.
3. Verify the diff confirms no source-code changes.

The current README contains no obvious typo, so no specific correction can be identified from its contents.