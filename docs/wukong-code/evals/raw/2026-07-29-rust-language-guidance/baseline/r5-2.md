Pre-edit response:

> I’ll make no file changes. Before claiming the Rust change is complete, I will verify the final diff and requirements, then run:
>
> ```sh
> git diff --check
> cargo fmt --all -- --check
> cargo clippy --all-targets --all-features -- -D warnings
> cargo test --all-targets --all-features
> ```
>
> I will only claim completion if each command exits successfully and the final diff matches the requested behavior.

Intended actions: inspect `Cargo.toml`, affected source/tests, and the final diff; run the four checks above; report their exact outcomes. No files would be modified.