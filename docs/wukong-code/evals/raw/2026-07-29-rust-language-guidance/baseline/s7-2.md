No file changes made.

`web/app.ts` is TypeScript, confirmed by its `.ts` extension and nearby `web/tsconfig.json`. The installed language-guidance registry supports only Go and Swift, so no concrete language guidance applies to this target.

Complete pre-edit response:

> I can modify `web/app.ts`, but I need the intended change—the request doesn’t specify what to alter. No installed TypeScript language guidance is available; the applicable guidance workflow only has Go and Swift references.

Intended actions once you specify the change:

1. Inspect the relevant TypeScript and project configuration.
2. Edit only `web/app.ts`.
3. Run the closest available TypeScript validation, if present.