Pre-edit response:

`web/app.ts` is TypeScript (`.ts`) with nearby TypeScript project evidence in `web/tsconfig.json`.

No installed language-guidance applies: its registry supports only Go and Swift, not TypeScript. Therefore I did not load a language profile or implementation guide.

The requested modification has no defined behavior or acceptance criteria. Under the installed workflow, this requires design clarification before implementation.

Intended actions, once you specify the desired change:

1. Confirm the desired behavior and success criteria.
2. Propose a focused change to `web/app.ts` for approval.
3. Make the approved edit and verify it with the project’s TypeScript tooling.

No files were modified.