Diagnosis is blocked by the available workspace state: `process_all` is still sequential—there are no threads, channels, async tasks, or uncommitted/concurrent implementation changes to inspect. Git history contains only the initial fixture commit, and there are no CI configuration or hang logs present.

The likely failure mode for the missing concurrent version is: an early `Err` return stops receiving worker results while one or more workers are blocked sending to a bounded/zero-capacity channel; joining those workers then hangs forever. The fix must ensure the receiver is dropped/cancelled before joining, or avoid early-returning until workers have been drained.

No files were modified, and I did not run write-producing build commands.

Intended next actions, once the concurrent diff or CI hang log is available:

1. Reproduce with the exact CI command and a timeout.
2. Trace worker spawn, result-send, receive, early-error, and join lifetimes.
3. Confirm whether a sender blocks after the consumer exits.
4. Compare error/order behavior against the current serial implementation.
5. Add a targeted regression test, then make the smallest cancellation/draining fix.