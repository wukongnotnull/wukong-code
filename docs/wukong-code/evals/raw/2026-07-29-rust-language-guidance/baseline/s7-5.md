No files were modified.

No installed language guidance applies to `web/app.ts`: it is TypeScript (`.ts`) with a nearby `web/tsconfig.json`, while the installed language-guidance registry supports only Go and Swift. The Rust `Cargo.toml` is in the separate `rust-worker/` subtree and does not apply.

Intended actions once you specify the desired change:

1. Inspect the relevant web configuration and surrounding code.
2. Edit only `web/app.ts`.
3. Run the applicable TypeScript validation, if available.