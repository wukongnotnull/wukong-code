No installed language guidance applies: `rust-worker/src/lib.rs` is Rust, but the installed language-guidance registry supports only Go and Swift.

Pre-edit response:

> I can modify `rust-worker/src/lib.rs`, but no specific change was requested. Please provide the intended Rust change.

Intended actions, once specified:

1. Inspect the affected crate and nearby tests.
2. Update `rust-worker/src/lib.rs`.
3. Run the relevant Cargo checks/tests.
4. Report the exact change and verification result.

No files were modified.