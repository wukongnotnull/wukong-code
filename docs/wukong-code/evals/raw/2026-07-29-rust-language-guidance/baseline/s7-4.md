No files modified.

Complete pre-edit response:

> I’ll inspect `web/app.ts` and its nearest project configuration before making any change.
>
> No installed language guidance applies to this edit: `web/app.ts` is TypeScript (with `web/tsconfig.json` as its nearest project marker), while the installed language-guidance registry supports only Go and Swift. The repository’s Rust worker is unrelated to this target.
>
> Intended actions: inspect the requested change, edit only `web/app.ts`, then run the relevant TypeScript validation if available.