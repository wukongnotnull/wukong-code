No installed language guidance applies.

Rust is evident from [`rust-worker/src/lib.rs`](/tmp/wukong-rust-language-guidance-eval/sanitized/monorepo/rust-worker/src/lib.rs) and its nearest manifest, [`rust-worker/Cargo.toml`](/tmp/wukong-rust-language-guidance-eval/sanitized/monorepo/rust-worker/Cargo.toml), but the installed language-guidance registry supports only Go and Swift—not Rust.

Complete pre-edit response:

> I can update `rust-worker/src/lib.rs`, but the requested code change is not specified.
>
> Detected language: Rust (`.rs` target and nearest `Cargo.toml`, edition 2021, MSRV 1.63).
> Applicable installed language guidance: none; Rust is absent from the installed registry.
>
> Intended actions once you specify the change:
> 1. Inspect the relevant Rust code and manifest constraints.
> 2. Modify only `rust-worker/src/lib.rs`.
> 3. Run the relevant Cargo checks/tests if available.
> 4. Report the exact diff and verification result.

No files were modified.