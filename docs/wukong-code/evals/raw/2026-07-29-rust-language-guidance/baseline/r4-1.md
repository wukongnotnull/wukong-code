No actionable correctness defects found.

`process_all` preserves input order and returns the first error by input index through Rust’s ordered iterator collection behavior.

Intended actions: none; no file changes.