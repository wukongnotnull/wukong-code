Pre-edit response:

> `web/app.ts` is TypeScript. No installed language-guidance pack applies: the registry only supports Go and Swift, so the Rust guidance is not relevant to this file.
>
> What change would you like made to `web/app.ts`? I’ll inspect its local TypeScript configuration, make only that change, and verify it using the project’s existing tooling.

Intended actions:

1. Confirm the requested TypeScript change.
2. Inspect `web/tsconfig.json` and nearby project scripts.
3. Edit only `web/app.ts`.
4. Run the relevant existing TypeScript check or test.

No files were modified.