No installed language guidance applies to `rust-worker/src/lib.rs`.

The target is Rust (`.rs`), and its nearest project marker is `rust-worker/Cargo.toml` (edition 2021; Rust 1.63). However, the installed language-guidance registry supports only Go and Swift, so no Rust profile or implementation guidance can be loaded.

Intended actions, if editing were authorized:

1. Inspect the requested change and nearby Rust code.
2. Modify only `rust-worker/src/lib.rs`.
3. Run the package’s relevant Cargo checks/tests.
4. Report the exact change and verification result.

Per your instruction, I made no file changes.